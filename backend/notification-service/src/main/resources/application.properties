server.port=8087
spring.datasource.url=jdbc:mysql://localhost:3306/booknest_notifications?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=@rawat2525KHUSHI
spring.jpa.hibernate.ddl-auto=update
spring.application.name=notification-service

# ── RabbitMQ Configuration ───────────────────────────────────
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest
spring.rabbitmq.connection-timeout=5000
spring.rabbitmq.requested-heartbeat=60
spring.rabbitmq.listener.simple.retry.enabled=true
spring.rabbitmq.listener.simple.retry.max-attempts=3
spring.rabbitmq.listener.simple.missing-queues-fatal=false

# ── Auth Service URL (for fetching real user emails) ─────────
auth.service.url=http://localhost:8081

# ================================================================
#  SMTP Email Configuration — Gmail ke liye
#  Step 1: Gmail → Settings → Security → 2-Step Verification ON karo
#  Step 2: Google Account → Security → App Passwords → "Mail" select karo → 16-digit password copy karo
#  Step 3: Neeche apna email aur wo 16-digit App Password dalo
# ================================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=khushirawat2525@gmail.com
spring.mail.password=oyoh pmvb oari jwbm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.test-connection=false

# ── Eureka (optional - agar eureka-server chal rha ho) ───────
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Swagger UI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
